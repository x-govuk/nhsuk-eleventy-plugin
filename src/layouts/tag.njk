{% extends "layouts/base.njk" %}

{% block content %}
  {{ appHeading({
    caption: eleventyCaption,
    title: title,
    description: description
  }) }}

  {{ appDocumentList({
    headingLevel: 3 if paginationHeading else 2,
    items: collections[tag] | reverse
  }) }}
{% endblock %}
